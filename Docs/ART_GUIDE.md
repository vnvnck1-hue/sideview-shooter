# 사이드뷰 슈팅게임 아트 가이드

> 누적 문서. 새 리소스를 제작하거나 피드백이 확정될 때마다 갱신한다.

## 1. 비주얼 방향

- 장르 표현: 어둡고 폐쇄적인 지하 공간의 사이드뷰 슈팅게임.
- 렌더링: 그림은 4px 블록 단위의 청키 픽셀 아트, 렌더는 풀해상도(1600×900). 조명·파티클·카메라 이동은 픽셀 격자에 묶지 않고 부드럽게 둔다 (2026-09-18 확정, §10).
- 형태 언어: 큰 사각 픽셀 덩어리, 단순하고 즉시 읽히는 실루엣, 제한된 내부 묘사.
- 분위기: 낡고 불안하지만 플레이 정보는 명확하게 읽혀야 한다.
- 화면 우선순위: 캐릭터 > 상호작용 가능한 프랍 > 배경 구조물.

## 2. 픽셀과 카메라

- 카메라는 사이드뷰 플레이를 기본으로 하되 프랍을 완전한 정사영 정면도로 평평하게 만들지 않는다.
- 프랍에는 윗면과 한쪽 측면이 소량 보이는 매우 약한 원근을 허용한다. 아이소메트릭 또는 강한 3/4 시점은 사용하지 않는다.
- 한 개 방을 보여줄 때 방 외곽은 화면 너비의 약 85%, 높이의 약 70%를 차지하도록 한다.
- 천장 위, 좌우 벽 밖, 바닥 아래에 좁고 차분한 암부 여백을 두어 디오라마의 외곽 구조가 읽히게 한다.
- 방이 화면 너비의 80%보다 작아지는 과도한 원경 구도는 사용하지 않는다.
- 위 85%/70% 는 컨셉 디오라마 한 장 구도 기준이다. 실제 게임 카메라(zoom 0.5, 가시 월드 3200×1800)는 단층 방(높이 560)을 화면 높이의 약 31% 로 보여주고, 마우스·시선 리드로 좌우로 움직인다. 위아래 암부 여백이 넓은 것은 의도이며, 허전하면 층고가 높은 방이나 원경 레이어로 채운다.
- 한 장면 안에서 캐릭터, 프랍, 배경의 픽셀 크기를 통일한다.
- 안티앨리어싱, 부드러운 브러시, 미세한 노이즈 텍스처를 사용하지 않는다.
- 형태는 작은 점들의 집합보다 큰 직사각형 클러스터로 묘사한다.
- 바닥은 플레이 가능한 연속 수평선으로 읽혀야 한다.

## 3. 컬러 역할 분리

배경과 프랍에 동일한 중간 명도와 동일한 자주색 계열을 사용하지 않는다.

### 배경 팔레트

- 차갑고 저채도인 청회색, 남보라, 차콜 계열을 사용한다.
- 프랍보다 명도 대비와 채도를 낮춘다.
- 균열, 벽돌, 파이프 같은 세부 요소는 넓고 조용한 색면으로 묶는다.
- 프랍 바로 뒤의 벽은 해당 프랍보다 어둡고 단순하게 유지한다.

### 프랍 팔레트

- 따뜻한 웜그레이, 먼지 낀 갈색, 벽돌색, 적갈색을 중심으로 한다.
- 최초 확정된 개별 프랍 리소스의 명도, 채도, 내부 음영을 표준 팔레트로 고정한다.
- 배경과 분리하기 위해 프랍 자체를 밝히거나 채도를 올리지 않는다.
- 대비가 부족하면 먼저 프랍 뒤의 배경을 더 차갑고 어둡고 단순하게 조정한다.
- 별도의 밝은 림을 추가하지 않으며 원본 프랍에 존재하는 하이라이트만 유지한다.
- 발광 외곽선이나 네온 테두리는 사용하지 않는다.

### 캐릭터 팔레트

- 캐릭터는 장면에서 가장 강한 실루엣 대비를 갖는다.
- 붉은 후드와 호박색 바이저는 **플레이어**의 핵심 식별색으로 사용한다. 일반 인간 NPC는 얼굴과 직업별 복장을 드러낸다.
- 캐릭터의 식별색은 넓게 남발하지 않고 작은 집중 영역으로 유지한다.
- 인간 NPC의 비율·얼굴·복장 규칙은 `Docs/CHARACTER_ART_GUIDE.md`를 따른다.

## 4. 조명

- 조명은 소수의 명확한 광원으로 구성한다.
- 따뜻한 조명은 작업 대상과 캐릭터 주변에 제한적으로 사용한다.
- 빛과 그림자는 부드러운 그라데이션 대신 단계적인 픽셀 클러스터로 표현한다.
- 블룸, 렌즈 효과, 흐림, 피사계 심도는 사용하지 않는다.
- 조명 때문에 프랍과 배경의 고유 팔레트가 다시 동일해지지 않도록 한다.

## 5. 프랍 설계

- 한눈에 기능을 알 수 있는 단순한 외곽선을 우선한다.
- 마모와 손상은 소수의 큰 패치와 긁힘으로 표현한다.
- 작은 장식이나 표면 노이즈보다 문, 손잡이, 서랍, 팔걸이처럼 기능을 설명하는 부위를 강조한다.
- 동일 공간의 프랍은 색의 온도와 외곽선 굵기를 공유한다.
- 투영 기준은 `workshop_cabinet_reference_projection_v2.png`와 `workshop_armchair_reference_projection_v2.png`다.
- 캐비넷과 소파처럼 정면 실루엣을 우선하되, 윗면·측면·팔걸이·다리의 깊이 차가 몇 픽셀 드러나는 약한 원근을 유지한다.
- 책상처럼 정면만 보이는 평면적 프랍은 기준 프랍과 같은 방향으로 윗면과 측면 깊이를 소량 추가한다.
- 프랍별 소실 방향과 바닥 접촉면을 일치시킨다. 원근을 없애서 접지 문제를 해결하지 않는다.

## 6. 환경 구성

- 방 하나는 하나의 연속된 플레이 공간으로 읽혀야 한다.
- 프랍 사이에 캐릭터가 이동할 수 있는 빈 바닥 공간을 남긴다.
- 천장 프레임, 벽, 바닥 구조는 방을 감싸되 프랍의 외곽선을 침범하지 않는다.
- 배경 디테일은 프랍이나 캐릭터보다 시선을 끌지 않아야 한다.

## 6-1. 캐릭터 애니메이션

- 모든 애니메이션 프레임은 동일한 셀 크기와 Bottom Center 피벗을 사용한다.
- 프레임의 최하단 발 또는 신체 접촉 픽셀을 셀의 공통 바닥선에 맞춘다.
- 무기나 팔이 좌우로 뻗어도 몸통과 바닥 피벗 위치는 이동시키지 않는다.
- 러프 키프레임 단계에서는 클립당 3~4프레임을 기본으로 하며 과도한 보간 동작을 추가하지 않는다.
- 아이들은 호흡과 후드·머리카락·소매·팔의 작은 변화만 사용한다.
- 걷기는 접지, 통과, 반대쪽 접지 자세가 명확히 구분돼야 한다.
- 사격은 준비, 반동, 최대 반동 또는 총구 화염, 복귀 순서로 읽혀야 한다.
- 숙이기는 서 있는 자세에서 낮은 실루엣으로 전환되며, 방향과 바닥 피벗을 유지한다.

## 7. 모듈형 룸 블록

- 방 하나를 게임 공간을 조립하는 기본 블록으로 취급한다.
- 각 블록은 완결된 천장, 좌우 벽, 연속 바닥과 어두운 외곽 여백을 가진다.
- 좌우 벽 끝은 인접 블록과 연결하기 쉬운 명확한 수직 경계로 정리한다.
- 테마마다 건축 배경과 프랍을 함께 설계하되 서로 다른 색상군으로 분리한다.
- 배경은 저채도·저대비의 차가운 색면, 프랍은 테마 고유의 따뜻하거나 식별성 높은 색군을 기본으로 한다.
- 색 분리는 발광 외곽선이나 프랍 밝기 증가가 아니라 색온도와 배후 단순화로 만든다.
- 장면에 캐릭터가 없어도 이동 가능한 바닥과 프랍 사이 통로가 명확히 읽혀야 한다.
- 블록마다 실루엣과 팔레트는 달라도 카메라 거리, 픽셀 밀도, 외곽 프레임 비율은 공유한다.
- 모든 방과 복도 블록은 동일한 상대적 바닥 높이를 공유한다.
- 좌우 연결부에는 배경 디테일과 프랍을 두지 않는 단순한 수직 도어 소켓 영역을 확보한다.
- 빈 복도는 테마 방 사이의 시각적 휴지 공간으로 사용하며, 가구나 이동을 방해하는 프랍을 배치하지 않는다.
- 연결 문은 방 팔레트에 종속되지 않는 중립적인 어두운 금속색을 사용한다.
- 문은 독립된 투명 스프라이트로 관리하며 방 또는 복도 블록의 도어 소켓 위에 배치한다.
- 현재 기본 연결 문은 닫힌 양분할 슬라이딩 방폭문이다.

### 문 유형

- 정면문은 뒤쪽 벽에 설치하며 다른 깊이의 공간이나 목적지로 이동하는 출입구다.
- 측벽문은 좌우 벽 경계에 설치하며 같은 평면에서 연결된 방과 복도 사이를 차단한다.
- 정면문은 넓은 정면 실루엣, 측벽문은 얇은 수직 실루엣으로 즉시 구분되어야 한다.
- 두 문은 동일한 중립 금속 팔레트를 공유할 수 있지만 형태와 배치 피벗은 별도로 관리한다.
- 측벽문 앞면을 가독성을 이유로 사선 또는 3/4 시점으로 노출하지 않는다.
- 닫힌 측벽문은 벽 단면과 일직선인 얇은 차단선, 잠금 상태등, 천장 수납부와 바닥 레일로 표현한다.
- 열린 측벽문은 문짝을 벽 속에 숨기고 프레임·레일·열림 상태등만 남겨 통로를 표시한다.
- 정면문은 Back Wall 레이어, 측벽문은 Side Boundary 레이어에 배치한다.

### 배경 타일

- 실내 배경의 첫 구현 단위는 천장부터 기초까지 포함하는 세로 매크로 타일로 한다.
- 타일은 좌측 캡, 중앙 벽 변형, 반복 벽, 우측 캡으로 나눈다.
- 중앙 벽 타일에는 조명·환기구·큰 균열을 매번 포함하지 않는다.
- 눈에 띄는 배경 요소의 연속 복제를 피하기 위해 무장식 변형과 좌우 반전 변형을 섞는다.
- 모든 타일은 동일한 바닥선과 높이를 공유한다.
- 프랍은 투명 스프라이트로 배경 타일과 분리하고 Bottom Center 피벗을 기본으로 한다.

### 프랍 접지

- 프랍의 수치상 바닥 좌표뿐 아니라 눈에 보이는 최하단 픽셀이 바닥과 맞닿아야 한다.
- 가구 다리와 캐릭터 발은 바닥 표면을 소량 침범하도록 개별 오프셋을 조정한다.
- 바닥 프랍에는 짧고 단단한 사각 픽셀 클러스터 형태의 접촉 그림자를 별도 레이어로 둔다.
- 접촉 그림자에 블러, 부드러운 타원, 반투명 그라데이션을 사용하지 않는다.
- 벽 부착물과 문에는 바닥 가구용 접촉 그림자를 적용하지 않는다.

## 8. 현재 기준 리소스

- 프랍 세트: `Assets/Generated/ChunkyPixelSet/`
- 최초 디오라마: `Assets/Generated/Environments/underground_workshop_diorama_v1.png`
- 팔레트 분리 실험: `Assets/Generated/Environments/underground_workshop_diorama_v2_palette_separated.png`
- 표준 프랍 팔레트 기준: `Assets/Generated/Environments/underground_workshop_diorama_v3_canonical_props.png`
- 현재 환경·카메라 기준: `Assets/Generated/Environments/underground_workshop_diorama_v4_subtle_zoomout.png`
- 모듈형 룸 블록 예시 — 수경재배실: `Assets/Generated/Environments/hydroponics_nursery_room_block_v1.png`
- 모듈형 룸 블록 예시 — 승무원 숙소: `Assets/Generated/Environments/crew_quarters_room_block_v1.png`
- 빈 연결 복도 블록: `Assets/Generated/Environments/empty_connector_corridor_block_v1.png`
- 기본 연결 문: `Assets/Generated/Connectors/bulkhead_sliding_door_closed_v1.png`
- 측벽 연결 문 원본: `Assets/Generated/Connectors/sidewall_shutter_closed_v1.png`
- 측벽문 닫힘 단면 기준: `Assets/Generated/Connectors/sidewall_shutter_closed_edge_v2.png`
- 측벽문 열림 단면 기준: `Assets/Generated/Connectors/sidewall_shutter_open_frame_v2.png`
- 게임 배치용 작업실 타일·프랍·문: `Assets/GameReady/`
- 긴 방 배치 검증: `Assets/GameReady/Validation/workshop_long_room_tile_prop_validation.png`
- 기준 이미지에서 분리한 약한 원근 프랍: `Assets/Generated/CanonicalProps/workshop_cabinet_reference_projection_v2.png`, `Assets/Generated/CanonicalProps/workshop_armchair_reference_projection_v2.png`
- 동일 투영으로 교정한 책상: `Assets/Generated/CanonicalProps/workshop_workbench_corrected_projection_v2.png`
- 캐릭터 애니메이션 마스터 시트: `Assets/GameReady/Characters/HoodedMechanic/Sheets/hooded_mechanic_all_4x4_v1.png`
- 캐릭터 애니메이션 메타데이터: `Assets/GameReady/Characters/HoodedMechanic/hooded_mechanic_animation_v1.json`
- 복도 배경 타일: `Assets/GameReady/Tiles/Corridor/`
- 수경재배실 배경 타일·프랍: `Assets/GameReady/Tiles/Hydroponics/`, `Assets/GameReady/Props/Hydroponics/`
- 승무원 숙소 배경 타일·프랍: `Assets/GameReady/Tiles/CrewQuarters/`, `Assets/GameReady/Props/CrewQuarters/`

v3는 프랍 색상 기준, v4는 카메라 거리와 환경 구성 기준으로 사용한다. v2는 프랍을 과도하게 밝힌 반례로 취급한다.

## 9. 금지 항목

- 배경과 프랍을 동일한 중간 명도·동일 색상군으로 통일하는 것
- 고해상도 픽셀 아트처럼 작은 픽셀로 표면을 과도하게 묘사하는 것
- 매끈한 그라데이션, 안티앨리어싱, 흐림, 블룸
- 완전한 정면 정사영으로 프랍의 깊이를 없애는 것
- 강한 원근, 아이소메트릭 시점, 프랍마다 다른 소실 방향, 불연속적인 바닥선
- 장식 때문에 캐릭터나 상호작용 프랍이 묻히는 구성

## 10. 네이티브 픽셀 규격 — 4px 블록 (**확정 · 자산 제작 규격**)

2026-09-18 렌더 프리셋 비교(픽셀레이트 이전 / 534×300 ×3 / 800×450 ×2 / 베이크 자산·풀해상도 / 원본 자산·2px 격자)로 **"베이크 자산 · 풀해상도"** 를 확정했다. 그림은 4×4 월드 px 단색 블록으로 정리된 픽셀 아트, 렌더는 1600×900 풀해상도 · 카메라 zoom 0.5. 저해상도 뷰포트(534×300 ×3), 계단형 광원, 8px 방식 B(267×150 ×6) 와 Power Relay 방은 폐기한다.

지금까지는 게임 스케일로 그린 그림을 `Tools/bake_pixel_grid.py` 가 4×4 블록 평균색으로 굽는 방식이었다(방식 A). 앞으로 새 자산은 **처음부터 네이티브 해상도(1 이미지 px = 1 아트 px)로 도트를 찍고 도구가 ×4 로 키워 넣는다**(방식 N). 결과는 화면에서 구분되지 않지만, 평균화로 세부가 녹는 일이 없고 팔레트와 격자를 작가가 직접 통제하며 파일 크기가 1/16 이다. 기존 베이크 자산과 섞어 써도 된다.

### 단위

| 단위 | 정의 |
|---|---|
| 아트 픽셀(art px) | 그림의 진짜 픽셀 1개. 도트 하나 |
| 월드 px | 게임 좌표. **1 art px = 4 월드 px** |
| 화면 px | 1600×900 창. **1 art px = 2 화면 px** (zoom 0.5) |

화면에는 가로 800 × 세로 450 art px 가 보인다. 방 좌표계·바닥선·타일 규격은 월드 px 기준 그대로다(RESOURCE_USAGE_GUIDE).

### 납품 형식 (방식 N — 권장)

- **네이티브 해상도로 그린다**: 이미지 1 px = 1 art px. 확대해서 넘기지 않는다.
- PNG, RGBA. 알파는 **0 또는 255 만**. 반투명·안티앨리어싱·부분 알파 금지 (반입 스크립트가 걸러낸다).
- 월드 크기는 4 의 배수 = 네이티브 크기는 정수. 아래 크기표를 따른다.
- 파일명과 하위 폴더는 게임 자산(`GodotPrototype/assets/`)과 같게 두고, 네이티브 원본은 `Assets/GameReady/Native4/<같은 상대경로>` 에 보관한다.
- 반입: `python Tools/upscale_native4.py` (×4 Nearest → `GodotPrototype/assets/`) → `python Tools/build_normal_maps.py` → `GodotPrototype/run.bat`(헤드리스 임포트). 베이크는 거치지 않는다.
- 격자 정렬은 신경 쓸 필요 없다. 네이티브로 그리면 격자가 곧 그림이다.

### 납품 형식 (방식 A — 기존, 게임 스케일 + 베이크)

게임 스케일(1 art px 를 4×4 px 덩어리로)로 그린 그림도 계속 받는다. `Tools/bake_pixel_grid.py` 가 4×4 블록(크롤러 10×10) 을 단색으로 굽고 알파를 다수결로 잘라 넣는다. 베이크가 잘 먹도록:

- 세부는 **4 월드 px(1 art px) 단위**로. 그보다 가는 선·점·글자는 평균색으로 녹아 사라진다. 확실히 남기려면 8 월드 px 이상.
- 타일은 128px 이음새 때문에 **격자 오프셋 0 고정**이다. 4px 격자에서 어긋나게 그리면 경계가 반 칸 밀린다. 프랍·캐릭터는 도구가 오프셋을 찾는다.
- 그라데이션·반투명·부드러운 브러시는 넣지 않는다. 명암은 2~3단계 색 블록으로 미리 나눈다.

### 크기 표 (네이티브 art px — 방식 N)

| 자산 | 월드 px | 네이티브 art px | 비고 |
|---|---|---|---|
| 모듈러 타일 (workshop_modular) | 128×128 | **32×32** | 시트도 32 배수. 3×2 시트 = 96×64 |
| 세로 매크로 벽 타일 | 256×560 | **64×140** | |
| 세로 매크로 캡 타일 | 160×560 | **40×140** | |
| 바닥선 (타일 상단 기준) | 486 | **≈121.5 → 122 (= 월드 488)** | 486 은 4 로 안 나눠진다. 새 타일은 밟는 띠 윗선을 488 에 두고, 코드 `FLOOR_Y` 도 488 로 옮긴다 (기존 베이크 타일은 오프셋 0 격자라 실제 경계가 이미 484/488 에 있다) |
| 캐릭터 프레임 셀 | 320×320 | **80×80** | 하단 중앙 피벗 유지 |
| 캐릭터 실루엣 (현재 160×267) | | 약 **40×67** | 화면 세로의 15%. 8px 규격(20×33)의 두 배 정보량 |
| 크롤러 (현재 543×756 셀, 0.4 배) | 실효 217×302 | **셀 64×48, 실루엣 약 52×32** | 새로 그릴 때는 스케일 1.0 (월드 256×192). `SCALE` 0.4 폐기, `MOUTH_LOCAL`·히트박스 재측정 |
| 캐비넷 | 370×315 | **92×80** (월드 368×320) | |
| 작업대 | 443×235 | **112×60** (월드 448×240) | |
| 소파 | 286×240 | **72×60** (월드 288×240) | |
| 정면문 (방폭문) | 315×330 | **80×84** (월드 320×336) | |
| 측벽 셔터 닫힘 / 열림 | 343×480 / 369×480 | **86×120 / 92×120** | 문 소켓 폭이 바뀌면 `room_data` 의 문 위치 재조정 |
| 파편·탄피·독액 등 이펙트 | 코드 생성 | **1 art px = 4 월드 px** 단위 | 코드 상수는 4 배수로 |

크기가 표와 다르게 필요하면 **월드 4 배수(네이티브 정수)** 만 지키면 된다. 바닥 접지 픽셀은 셀의 공통 바닥선에 맞춘다(§5, §6).

### 그리는 법 (art px 하나 = 화면 2px 에서 지킬 것)

- **외곽선은 1 art px 로 전체를 둘러도 된다.** 화면 2px 라 답답하지 않다. 캐릭터·상호작용 프랍은 실루엣 대비를 위해 두르고, 배경 타일은 어두운 쪽(접촉면·그늘)에만 두어 프랍과 위계를 나눈다.
- **세부는 1 art px 부터 허용, 장식 점은 2×2 권장.** 1 px 볼트·긁힘은 화면 2px 라 읽히지만 많으면 노이즈다. 균열은 폭 1·길이 4 이상.
- **팔레트는 자산당 8~16색.** 램프(shade) 3단계 + 하이라이트 1 + 외곽선 1 로 묶는다. 배경 청회색 계열과 프랍 웜그레이 계열 분리는 §3 그대로.
- **음영은 계단.** 디더링(체커)은 넓은 면의 2단계 경계에 한 줄까지만. 화면 2px 격자라 8px 규격보다는 튀지 않지만 §9 의 "매끈한 그라데이션 금지" 는 유지.
- **글자는 그리지 않는다.** 32 px 타일에 읽히는 글자를 넣을 수 없다. 표지판은 색 블록과 아이콘 실루엣으로.
- **플레이어와 얼굴을 가린 인물**은 바이저 3×1~4×2 art px 정도로 식별한다. **일반 인간 NPC**는 얼굴을 드러내되 눈 1개·코·턱을 소수의 큰 픽셀 클러스터로 표현한다. 세부 기준은 `Docs/CHARACTER_ART_GUIDE.md`.
- **애니메이션은 프레임으로.** 1 art px 이동이 화면 2px 라 미세 떨림은 크지 않지만, 대기 호흡·걷기 모두 코드 스케일 대신 프레임으로 그린다. 걷기 4프레임은 다리 위치가 1 px 이상 바뀌게.
- 벽 타일은 **이음새 격자에 무늬를 맞춘다.** 32 px 안에 벽돌 2×1 이 정확히 4줄처럼 타일 경계에서 무늬가 나누어지게. 4방 타일링 검사는 네이티브 크기에서 한다.

### 조명·이펙트 (코드 쪽)

- 광원 고리(`Lighting.radial_texture`)·글로우·파티클·카메라 이동은 풀해상도에서 부드럽게 둔다. 픽셀 격자에 스냅하지 않는다 — 그림만 픽셀이고 빛과 움직임은 매끄러운 것이 이 스타일의 핵심.
- 코드가 월드 px 로 그리는 것(조준점, 파편, 탄피, 불 셰이더 `pixel_step`)은 4 배수로 두어 art px 격자와 맞춘다.

### 이행 순서

1. **코드 확정**: 렌더 프리셋을 "베이크 자산 · 풀해상도" 하나로 고정하고 저해상도 뷰포트·계단형 광원·뷰 px 상수·Power Relay 방·Native8 자산을 제거한다. `assets_original/` 비교 폴더도 삭제한다(원본은 커밋 314190d 에서 언제든 꺼낼 수 있다).
2. **기존 자산은 그대로**(베이크본 유지). 다시 그릴 필요 없다.
3. **새 자산부터 방식 N**: `Assets/GameReady/Native4/` 에 네이티브로 그려 `Tools/upscale_native4.py` 로 반입. 기존 자산을 교체할 때도 같은 파일명으로 넣으면 좌표·타일셋은 건드리지 않는다.
4. 우선순위: 베이크에서 가장 뭉개졌던 순서 — ① 문 손잡이·캐비넷 디테일 등 프랍 ② 벽 표지판(글자 → 색 블록) ③ 캐릭터 분리 프레임 ④ 크롤러(새 셀 64×48, 스케일 1.0) ⑤ 타일. 넓은 색면 프랍(소파·작업대)은 베이크본으로 충분해 마지막.

## 변경 기록

- v0.1: 청키 저해상도 픽셀 아트, 제한 팔레트, 사이드뷰 공간 구성 규칙 수립.
- v0.2: 배경은 차가운 저채도 팔레트, 프랍은 따뜻하고 더 밝은 팔레트로 분리. 화면 위계 규칙 확정.
- v0.3: 프랍을 밝히는 규칙 폐기. 최초 개별 프랍의 표준 팔레트를 고정하고, 가독성은 배경 팔레트만 조정해 확보하도록 수정.
- v0.4: 방 외곽이 적당히 보이는 가벼운 원경 구도를 채택. 방 너비 약 85%, 높이 약 70%를 카메라 기준으로 추가.
- v0.5: 방 하나를 조립 가능한 게임 공간 블록으로 정의. 테마별 배경·프랍 색상군 분리와 공통 연결 경계 규칙 추가.
- v0.6: 공통 바닥선, 좌우 도어 소켓, 빈 연결 복도와 독립형 투명 방폭문 규칙 추가.
- v0.7: 정면문과 측벽문을 분리 정의. 작업실 세로 매크로 타일, 프랍 공통 스케일, 반복 변형과 실제 긴 방 배치 검증 규칙 추가.
- v0.8: 프랍별 바닥 오프셋과 픽셀 접촉 그림자 규칙 추가. 측벽문의 사선 앞면 표현을 폐기하고 단면형 닫힘·열림 상태로 교체.
- v0.9: 기준 이미지의 캐비넷·소파를 표준 투영으로 지정. 완전한 무원근 정면도가 아니라 윗면과 측면이 조금 보이는 약한 원근을 프랍 공통 규칙으로 확정하고 책상을 같은 투영으로 교정.
- v1.0: 아이들·걷기·사격·숙이기 최소 키프레임 규칙 추가. 모든 캐릭터 프레임에 동일한 셀 크기, 하단 중앙 피벗과 공통 바닥선을 적용.
- v1.1: 빈 연결 복도, 수경재배실과 승무원 숙소를 작업실과 같은 세로 매크로 타일 규격으로 확장. 테마별 빈 배경과 투명 프랍 분리 및 긴 방 재조립 검증 추가.
- v1.2: 저해상도 렌더링 전환(534×300 ×3, 4px 블록 베이크) 반영. 다음 단계 후보 "방식 B" 8px 네이티브 픽셀 규격(§10)을 예정·미적용으로 추가.
- v1.3: 방식 B 를 기존 그림의 8px 수식 축소로 시험(267×150 ×6)했다가 롤백. 가는 요소가 뭉개져 네이티브 직접 제작으로 결정. §10 을 자산 제작 규격으로 정리하고 제작 우선순위 추가.
- v1.4: 렌더 프리셋 5종 실시간 비교 후 **"베이크 자산 · 풀해상도"(4px 블록 그림 + 1600×900 풀해상도 렌더, zoom 0.5)** 확정. 저해상도 뷰포트·계단형 광원·8px 방식 B·Power Relay 방 폐기. §10 을 4px 네이티브 규격(방식 N: 1 art px = 월드 4px = 화면 2px, `Assets/GameReady/Native4/` + `Tools/upscale_native4.py`)으로 교체하고 베이크 방식 A 의 제작 규칙 병기. §1 렌더링 정의, §2 실제 게임 카메라 구도 주석 추가.
