class_name DepthLayers
extends RefCounted
## 공간감(층 분리) 규격 — 방을 몇 겹으로 쌓고 각 층이 조명을 얼마나 받는가.
##
## 2026-09-20 확정: **근경 분리로 고정**한다. 예전에는 "이전(평면) ↔ 근경 분리" 두 프리셋을
## F2 로 A/B 비교했지만(같은 방에서 Main 재로드), 근경 쪽으로 결론이 나서 프리셋 표와 전환을 걷어냈다.
## 남은 것은 층 번호와 조명 비율 — 이건 프리셋이 아니라 렌더 규격이라 여기 그대로 둔다.
##
## 확정된 구성:
##   먼지·빛 기둥은 벽과 인물 사이(z3~4)에 있고, 벽 램프·채광·비상등은 배경 층(z≤4)만 정면으로 비춘다.
##   인물 층(z5~6)은 55%(바닥 풀·바닥 라이트는 75%) 의 거울 라이트(LightMirror)가 비춘다.
##   근경 실루엣 층(ForegroundLayer, z7)은 몸체가 라이트를 받지 않고 윤곽 띠만 림으로 반응하며,
##   플레이어 이동의 1.045배로 움직여 카메라 앞을 스쳐 간다.

## 층 번호 (Room 이 정하는 z_index 와 맞춘다)
const Z_BACK_MAX := 4        # 배경 층: 타일 0 · 뒷벽 문 1 · 프랍 2 · 먼지 3 · 빛 기둥/환경 연출 4
const Z_ACTOR_MIN := 5       # 인물 층: 플레이어·몬스터 5 · 탄·탄피 6
const Z_ACTOR_MAX := 6
const Z_FOREGROUND := 7      # 근경 실루엣

## 인물 층이 받는 벽 조명 비율 (배경 층 = 1.0)
const ACTOR_LIGHT_RATIO := 0.55
const ACTOR_FLOOR_LIGHT_RATIO := 0.75
